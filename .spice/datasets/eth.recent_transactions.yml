name: eth.recent_transactions
type: append
# firecache:
#   enabled: true
#   trigger: block_number
#   time_column: block_timestamp

migrations:
  - name: create_view
    access:
      read:
        - 'lukekim'
        - 'spiceai'
    acceleration:
      type: raw
      columns:
        - name: block_number
    sql: |
      CREATE VIEW ewgenius.lukekim-demo.recent_transactions AS
        SELECT * FROM eth.recent_transactions;
